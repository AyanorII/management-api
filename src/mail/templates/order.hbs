<h1>{{user.name}}</h1>
{{#if order}}
  <h2>Order #{{order.id}}</h2>
  <p>Order total: {{order.total}}</p>
  {{#each order.items}}
    <p>{{this.name}} - {{this.price}}</p>
  {{/each}}
{{/if}}

{{! Path: src/mail/templates/order.js }}
